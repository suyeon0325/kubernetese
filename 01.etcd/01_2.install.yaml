## 실행
## etcd를 실행하면 기본 적으로 **2379** 포트에서 수신 대기하는 서비스가 시작됨
  ## 시스템 서비스 or 클러스터 파드로 실행이 이상적

## 그 후 클라이언트를 etcd서비스에 연결하여 정보를 저장하고 검색할 수 있음

--

## 데이터베이스에 저장된 데이터를 검색할 수 있는 정보가 포함된 항목 생성
./etcdctl put key1 value1

## key1에 저장된 값 가져오기 
./etcdctl get key1

## 사용 가능한 명령어 확인하기
./etcdctl